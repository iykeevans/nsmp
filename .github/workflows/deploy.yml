name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ASIAZQ3DTTJY343ELR6G
          aws-secret-access-key: rIQFUBsgrIUJyVP79dbn8tmQRK1+M8c/H/GRNZ0+
          aws-region: us-east-1

      - name: Deploy static site to S3 bucket
        run: aws s3 sync ./public/ s3://nsmp-iyke --delete
